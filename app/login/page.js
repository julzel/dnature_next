import Login from '../../features/Login';

export const metadata = {
  title: 'Login',
  robots: {
    index: false,
    follow: false,
  },
};

const LoginPage = () => <Login />;

export default LoginPage;
