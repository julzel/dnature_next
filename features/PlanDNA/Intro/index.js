import React from 'react';

// local imports
// components
import PlanIntro from './PlanIntro';

const PlanIntroContainer = ({ handleStartClick }) => (
  <PlanIntro handleStartClick={handleStartClick} />
);

export default PlanIntroContainer;
